{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "scripts": {
    "bootstrap": "npx lerna bootstrap",
    "test": "npx lerna run test --stream",
    "lint": "npx lerna run lint --stream"
  },
  "version": "1.45.1"
}
