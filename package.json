{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "scripts": {
    "bootstrap": "npx lerna bootstrap",
    "test": "npx lerna run test --stream"
  },
  "version": "1.4.0"
}
