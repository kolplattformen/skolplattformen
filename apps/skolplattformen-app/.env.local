# Dev-session för Infomentor (gitignored!). Kopiera DEV-SESSION-raden
# som libs/api-infomentor/scripts/e2e-qr-server.ts skriver ut efter en
# lyckad QR-inloggning. Kräver omstart av Metro efter ändring.
# Dev-session for Infomentor (gitignored - ALDRIG committa).
# IMHome galler ca 40 min: ga tillbaka till e2e-qr-server-flodet och
# klistra in den nya minimala DEV-SESSION-raden har.
# OBS: Metro maste startas om med -c efter ndring (EXPO_PUBLIC inline:as).
# Dev-session för Infomentor (gitignored - ALDRIG committa).
# IMHome gäller ca 40 min: kör e2e-qr-server-flödet och klistra in den
# nya minimala DEV-SESSION-rad här.
# OBS: Metro måste startas om med -c efter ändring (EXPO_PUBLIC inline:as).
#
# [2026-09-05 kväll] Avslagen nu: resume/silent-refresh-flödet tar hand om
# sessionerna (se libs/api-infomentor/lib/index.ts + api.resumeSession -
# sessionen återupptas ur den nativa cookie-store:n, död session => tyst
# refresh med BankID-push, fallback login-skärm). Återaktivera genom att
# klistra in en DEV-SESSION-rad nedan om du vill slippa BankID i test.

